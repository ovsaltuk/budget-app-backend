{
  "compilerOptions": {
    "target": "ES2020", //Компилировать код в стандарт JavaScript ES2020. Это современный стандарт, хорошо поддерживаемый Node.js.
    "module": "commonjs", //Использовать систему модулей CommonJS. Это стандарт для Node.js (require/module.exports).
    "lib": ["ES2020"], //Библиотеки, которые будут доступны во время компиляции.
    "outDir": "./dist", //Папка, куда компилятор будет помещать сгенерированные .js файлы.
    "rootDir": "./src", //Папка, где лежат наши исходные .ts файлы. Всю разработку мы будем вести в папке src.
    "strict": true, //Включает весь набор строгих проверок типов. Это лучшая практика, она помогает отловить огромное количество потенциальных ошибок.
    "esModuleInterop": true, //Позволяет корректно импортировать CommonJS-модули (которые используют module.exports) как ES-модули (которые используют import ... from).
    "skipLibCheck": true, //Пропускает проверку типов в файлах деклараций (.d.ts) в node_modules. Это ускоряет компиляцию.
    "forceConsistentCasingInFileNames": true //Обеспечивает согласованность регистров в именах файлов.
  },
  "include": ["src/**/*"], // Включает все файлы .ts в папке src и ее подкаталогах
  "exclude": ["node_modules", "dist"] // Исключает папки node_modules и dist из компиляции
}